# Copy this file to .env and set your values
# This file is not used directly by the app.
# The real file should be named `.env` (which is git-ignored).

# OpenAI API key for API backend transcription
OPENAI_API_KEY=your_openai_api_key_here
